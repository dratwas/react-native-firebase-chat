/*
** @flow
*/

import { WHITE, BACKGROUND_DARK_BLUE } from './colors';

const navigatorConfig = {
  navigationBar: {
    backgroundColor: BACKGROUND_DARK_BLUE,
    tintColor: WHITE,
  },
};

export default navigatorConfig;
